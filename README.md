# cust_dev_ai

## Langfuse tracing setup

1. Ensure the Langfuse SDK configuration lives in `config/.env`. Provide the public/secret keys plus the host and base URL that point at your local Langfuse deployment.
